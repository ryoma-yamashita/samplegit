var boolean = true;
// var resultText = "";
// if (boolean) {
//   resultText = "Aの処理";
// } else {
//   resultText = "Bの処理";
// };
var resultText = boolean ? "Aの処理" : "Bの処理"
console.log(resultText);

var resultNum = 5 > 6 ? true : false;
console.log(resultNum);

var boolean = true;
var text = boolean ? "trueの処理" : "falseの処理"
console.log(text);

var numA = 10;
var numA = numA > 5 ? true : false;
console.log(numA);

var numB = 3;
var numB = numB < 5 ? true : false;
console.log(numB);

var numC = 12;
var numC = numC >= 10 ? true : false;
console.log(numC);

var numD = null;
var numD = numD == null ? true : false;
console.log(numD);